export type Multa = {
    multaId: number,
    monto: number,
    razon: string,
    emitida: Date,
}

export type MultaResDTO = {
    MultaId: number,
    Monto: number,
    Razon: string,
    Emitida: Date,
}
